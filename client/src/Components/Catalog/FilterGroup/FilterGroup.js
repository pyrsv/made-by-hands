// import React from 'react';
// import PropTypes from 'prop-types';
// import Checkbox from '../../UI/Checkbox/Checkbox';
// import { FiltersWrapper, FiltersName } from './styles';

// const FilterGroup = ({ name, fields, values, valuesKey, onChange }) => {
// 	return (
// 		<FiltersWrapper>
// 			<FiltersName>{name}</FiltersName>
// 		</FiltersWrapper>
// 	);
// };

// FilterGroup.propTypes = {
// 	name: PropTypes.string.isRequired,
// 	fields: PropTypes.arrayOf(PropTypes.object).isRequired,
// 	values: PropTypes.objectOf(PropTypes.bool).isRequired,
// 	valuesKey: PropTypes.string.isRequired,
// 	onChange: PropTypes.func.isRequired,
// };

// export default FilterGroup;
